apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-sample
  namespace: sample
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 10Gi
